The running AMAAZE mCT Workflow "To Do" List

---------------------------------------------------------------------------------------------------------------------
Immediate goals
---------------------------------------------------------------------------------------------------------------------

- Run 04 and 05. Make sure the meshes are meshing.

- Remove the ISO value detection. We'll deal with that later.
    - Do make sure that we request ISO value at the end of 03_segment.py
    - Later when we get the ISO-value detection running properly we can simply have it run on the subvolume and offer a print statement with a suggested value. The user will still type the one they want whether suggestion or their own.

- Set all steps up so that the user can initialize the subequent step. 
    - Put all user interface from 04 at the end of 03 so that 04 can batch process
    - When 04 is initiated, give the user the option to choose multiple output paths
    - When batch processssing is chosen, 05 should auto-intiate so the user can return to both the original and clean meshes. 

- Make sure that the manual override for each cell segmentation follows immediately after its companion automated suggestion.

- replace stability_image lable with local_homogeneity_image

- Go through the workflow and identify the confusing moments then fix them  

- Make sure to skip divider detection when expected_n_dividers == 0

- 04 should print progress after each specimen, something like “writing volume 3/8…” and “surfacing mesh 3/8…”, because file-size watching should not be the only proof that the script is alive.

- Identify all print statement outputs and graphical interface outputs that aren't making their way into the metadata json
    - Identify what you want to keep and incorporate that into the flow.

- Update the md template to separate extraction and surfacing times in the summary and in the descriptive part separate further: extraction, write npy, etc. because we were getting stalledin the npy writing, although this seems to be a product of the z-averaging. 

- Make sure that wherever possible when there is (or should be) a runtime error that this is changed to an interactive moment with the user so they don't necessarily have to rerun the entire script.

- Add dataset names to all interface graphics!

- Fix occupancy graph by adding blue bands even when single candidates and adding the blue bands to the legend.

- 04 should print progress after each specimen, something like “writing volume 3/8…” and “surfacing mesh 3/8…”, because file-size watching should not be the only proof that the script is alive.

- rework 00's rotation interface so that horizontal and vertical guidelines show up at the center of the image and either the user can click on the image and rotate it such that their dividers appear parallel to the guidelines as a means for assessing degree of rotation, or a separate window pops up with both a slider and window with up/down arrows next to it and in the window is the current rotation angle and the user can use the slider or type into the window or advance with the arrows to give rotation. As opposed to an iterative guess input affirm loop. 


---------------------------------------------------------------------------------------------------------------------
Less immediate goals
---------------------------------------------------------------------------------------------------------------------

- Once 03 is sussed, run the different tests, checking times, quality, etc. 
    - Vary the z-window value
    - Identify any other variations you may wish to test.

- update the photo creation script to align with workflow scripts

- update the contact sheet script to align with worfklow scripts
    - Is it possible to automate the running of the image script when this script is invoked since that one is required for this one? Otherwise, interactive print message works. 

- create a .gif creating script

- create a reproduce_workflow script so the run_metadata.json has purpose. 
    - Add an immediate, before continuing with this script make sure you rename the output folders from previous runs so you don't overwrite anything print statement. 

- clean up and incorporate the the dicom to jpg conversion script. 

- Update script header descriptions

- Make sure to update documentation and paper to reflect that there should be no scan number column in the CSV and that there should be one CSV per scan package. 

- Add documentation in setup instructions telling users to install Pandoc separately in order for report generation to work succesfully. 

- Review and update all documentation and api across the board as this is all going to have to change. 






