First: broaden slice loading so the workflow accepts .tif/.tiff/.dcm/.dicom stacks through one shared helper. The helper should return sorted slice filenames and read each slice appropriately. For DICOM, assume slice order follows filename/index order unless later we decide to read DICOM metadata for slice position. That change probably belongs in utils.py, then 01/02 use the same loader.

Second: make 01 interactive. The user should give only scanpath, slicepath, scan_num, maybe layoutfile, and 01 should guide them through preview orientation: show representative slice, ask transpose yes/no, allow rotation selection, allow crop box selection, then write ang2rot, rowrng, colrng, transpose_preview, and slice-file metadata to run_metadata.json and/or updated config output.

For rotation, yes, it is possible. The simplest version is not “click and drag rotate,” but show the image and let the user enter a trial angle, redraw, repeat until happy, then press Enter to accept. More intuitive later would be two clicks along a box edge/divider edge, compute the angle from that line, rotate to make it horizontal/vertical, and show the result.

I’d prioritize in this order: universal slice loader first, interactive crop second, transpose prompt third, rotation-by-two-clicks fourth. That reduces JSON overwhelm fast without tangling everything at once.

Here is a workflow for surfacing that goes across several scripts and uses utils. First, we need to broaden slice loading so the workflow accepts .tif/.tiff/.dcm/.dicom stacks through one shared helper. The helper should return sorted slice filenames and read each slice appropriately. We are assuming that the slice order follows and ordered index. However, they numbers do not need to be continugous. In other words, 0, 1, 2, 3, 4, and 1, 2, 5, 8, would be acceptable, whereas, 2, 0, 8, 1 would not. Please read through each .py file and let me know what needs to be deleted, added, or changed for this to work properly across all scripts. 
